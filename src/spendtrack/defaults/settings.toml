port = 8766

[llm]
# Режимы LLM (резолв — resolve_providers() в llm.py; статус — `spendtrack llm-status`):
#   • по умолчанию выключен: без ключей/BYO сеть не трогается вообще;
#   • BYO/Ollama (рекомендуется): SPENDTRACK_LLM_BASE_URL/MODEL/API_KEY или SPENDTRACK_LLM_PROVIDER=ollama —
#     единственный провайдер без фолбэков: данные идут ровно туда, куда указал пользователь;
#   • free-цепочка ниже — осознанный opt-in (ключи; локальные шимы — ещё и SPENDTRACK_ALLOW_LOCAL_LLM=1).
primary = { base_url = "https://openrouter.ai/api/v1", model = "nvidia/nemotron-3-super-120b-a12b:free" }
fallback = { base_url = "http://localhost:3001/v1", model = "glm-4.5-flash" }
deepseek = { base_url = "http://127.0.0.1:3201/v1", model = "deepseek-v4-1-flash" }
# offline: модель для пресета SPENDTRACK_LLM_PROVIDER=ollama (полностью локально, air-gap)
offline = { base_url = "http://127.0.0.1:11434/v1", model = "qwen2.5-coder:3b" }
max_tokens = 400

[acceptance]
auto_accept_confidence = 0.9