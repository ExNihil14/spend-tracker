# Приватность spend-tracker

Коротко: по умолчанию приложение **не делает ни одного сетевого вызова** и не имеет телеметрии.
Данные (SQLite-файл `data/spend.db`) лежат только на вашей машине и не шифруются на уровне приложения.

## Что происходит без настройки (по умолчанию)

- HTTP-сервер слушает только `127.0.0.1` (localhost) — снаружи недоступен.
- LLM **выключен**: без ключей ни один провайдер не вызывается, сеть не трогается вообще.
  Неизвестные категоризатору операции уходят в очередь «Подтвердить» к человеку.
- Это проверяется тестом: `uv run pytest tests/test_offline.py` (сокеты за пределами localhost блокируются;
  импорт, дайджест, doctor и страницы работают офлайн).

## Что и когда уходит наружу (только если вы включили LLM)

LLM включается осознанно: ключи `SPENDTRACK_OPENROUTER_API_KEY` и/или `SPENDTRACK_FREEL_LLM_API_KEY`
(например, в `.env`). Тогда для операций, которые не закрыты правилами, отправляется:

- описание операции (усечено до 300 символов),
- сумма,
- псевдоним счёта (`acc_xxxx`, не номер карты),
- дата,
- до 8 few-shot примеров из ваших правок (описание, сумма, категория) — они помогают модели.

Провайдеры (задаются в `config/settings.toml`; ключи — только в env, в git не попадают):
1. OpenRouter (`https://openrouter.ai/api/v1`) — основный;
2. FreeLLMAPI (`http://localhost:3001/v1`) — локальный прокси к внешним моделям;
3. abacus-web shim (`http://127.0.0.1:3201/v1`) — локальный прокси к внешним моделям (нужен `SPENDTRACK_ALLOW_LOCAL_LLM=1`);
4. офлайн-эндпоинт Ollama (`http://127.0.0.1:11434`) — полностью локально.

Важно: локальные прокси (п. 2–3) тоже передают данные внешним сервисам — поэтому они включаются
только явным флагом `SPENDTRACK_ALLOW_LOCAL_LLM=1` и не участвуют в офлайн-режиме по умолчанию.
Prompt-injection защищён валидацией: категория вне таксономии отвергается и строка уходит в очередь.

## Как выключить / проверить

- Выключить: удалите ключи из окружения/`.env` — LLM-вызовы прекратятся, правила и офлайн-кэш продолжат работать.
- Проверить отсутствие утечек: `uv run pytest tests/test_offline.py` (в CI — всегда).
- Бэкапы (`scripts/backup.py`, VACUUM INTO) локальные; копию желательно хранить вне ноутбука
  (внешний диск или папка вашего облака — это ваше решение, приложение само никуда не отправляет).

## Чего приложение не делает

- Не отправляет телеметрию/аналитику, не проверяет обновления по сети, не «звонит домой».
- Не хранит сырые номера карт (только псевдонимы `acc_xxxx`).
- Не шифрует БД — если нужна защита файла, используйте BitLocker/права доступа ОС.
